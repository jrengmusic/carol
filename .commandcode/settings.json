{
  "permissions": {
    "deny": [
      "Shell(rm -rf *)",
      "Shell(git push --force*)",
      "Shell(git reset --hard*)",
      "Shell(git checkout --*)",
      "Shell(git branch -D*)"
    ]
  }
}
