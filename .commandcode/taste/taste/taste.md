# Taste
- Prefers to discuss and align on the approach before implementation ("discuss first") rather than having the agent build things immediately. Confidence: 0.8
- Prioritizes protocol discipline and operational behavior — rules enforcement, communication style, role delegation, and autonomous tool workflows — over tooling/integration plumbing (e.g., CLI subcommands, installer wiring). Confidence: 0.7
- Avoids Anthropic models when using Command Code; wants model choices mapped to the non-Anthropic models actually available on their plan, grounded in the real catalog (e.g., `cmd --list-models`) rather than docs tables or priors. Confidence: 0.9
- Prefers cost-conscious model allocation: cheaper/faster models for routine and discovery roles rather than frontier-tier everywhere; for discovery roles (Librarian/Pathfinder) "any capable model with the most efficient economy" is fine. Actively participates in the role→model mapping but will defer to the agent's efficiency analysis over their own initial picks when the agent demonstrates better cost-efficiency. Extends economy-tier allocation beyond discovery roles, picking the cheapest capable option (deepseek-v4-flash) for the MACHINIST executor role. Confidence: 0.95
- Prefers starting with cheaper, adequate model seats for new setups and iterating later based on hands-on performance ("for starter... we could tweak later, after i have experienced the performance") rather than committing to expensive frontier tiers up front. Confidence: 0.8
- Trusts providers/models they have hands-on experience with (e.g., GLM, initially for auditing) over unproven ones; hesitant to pay more for models they haven't validated with their workflows — especially since their sessions are long and token-hungry. Hands-on GLM trust has expanded to explicitly assigning GLM (`zai-org/GLM-5.3`) to the primary COUNSELOR seat. Confidence: 0.85
- Expects claims about tool capability boundaries to be verified against ground truth (authoritative docs and empirical CLI testing) rather than assumed — e.g., probing whether `effort` can be set per agent role at all, or only on the primary session via CLI flags; wants precise answers about what is and isn't supported. Confidence: 0.7
- Prefers deterministic, no-fallback model pinning: the model written on the agent's frontmatter is authoritative and should always be used ("no need fallback for cmd code, counselor model always what written on its frontmatter") — rejects fallback-ladder complexity in favor of a single declared seat. Confidence: 0.7
- Prefers a quiet feed: no `showOutput` on delegated agents, subagent work stays compact, and delegation visibility is treated as a nice-to-have curiosity rather than something to surface verbosely in the UI. Confidence: 0.7
- Prioritizes a strong single source of truth: wants the canonical CAROL structure kept intact as-is with only a thin layer of overrides on top that Command Code reads — explicitly preferring this over full copies/parallel directories ("way better"), because it guarantees everything stays in sync no matter which CLI is used and avoids drift/duplication. Confidence: 0.9
- When porting assets to another tool/CLI, prefers to reuse existing portable assets as-is (e.g., skills and commands that work unchanged, wired via symlinks) rather than translating or duplicating them; only the genuinely incompatible layer (like agent frontmatter model/tool names) gets translated. Confidence: 0.8
- Prefers pragmatic, explicitly-temporary scaffolding decisions (e.g., placing the port script at `~/.carol/bin/carolcmd` "temporarily for now") and minimal initial builds — approving a v1 that is just the port script + bootstrap while deferring optional extras (like a delegation log) to later — over committing to final structure or features up front. Confidence: 0.6
- Wants the primary role adopted automatically by default rather than manually per session — asked for every `cmd` launch to "always" come up as COUNSELOR (default model + memory-role block + SessionStart hook), with other roles as explicit overrides. Prefers zero-friction, always-on defaults over requiring a per-session invocation step. Confidence: 0.8
- Shell tooling must be bash 3.2–compatible (macOS bundled bash): no associative arrays, no heredoc inside command substitution (`$(cat <<'EOF' ...)` mis-parses), and single-quoted strings must avoid bare apostrophes; `read -r -d '' <<'EOF'` is the safe pattern for multi-line string literals. Confidence: 0.7
- Communicates with terse directives — single-word commands ("push") or short imperatives ("fix both issues") — and expects the agent to recognize the established protocol/context and execute the full workflow autonomously (e.g., stage all, commit, push; or investigate and resolve multiple flagged issues in one pass) with no confirmation round-trip. Confidence: 0.75
- Prefers role slash commands to be thin, prompt-only stubs kept in the SSOT (`commands/<role>.md`): frontmatter description + "Activate as X. Full role definition is in the X agent." + a MANDATORY acknowledgment line + "wait for ARCHITECT direction, do not start working" — pointing at the full agent file rather than embedding role definitions, and wired into the target tool via symlinks. Confidence: 0.6
- When a mechanism needs data that is already config/frontmatter-driven, prefers reading from that single source at runtime rather than hardcoding a lookup table, so that text/color/name all derive from the same defining file and cannot drift. Confidence: 0.6
- Expects a permissive "allowed by default, except gated by deny" permission model in Command Code: ordinary tool calls should not prompt (wants `permissions.defaultMode` = `auto-accept`), with explicit deny rules and destructive-operation guards as the only gate — matching CAROL's Claude Code behavior rather than `default`/ask mode. Reconfirmed when explicitly demanding the Claude Code destructive-command deny list (git reset --hard / checkout -- / branch -D) also be applied to Command Code via a deterministic PreToolUse hook, and again when he asked to "allow by default always globally" and assumed destructive commands are already denied. Confidence: 0.95
- Wants an at-a-glance session status line in the tool UI showing CAROL version, active role, and model (e.g., `◈ CAROL v0.0.24   COUNSELOR   Fable 5`), and prefers surfacing context-window usage (rot meter) and rate-limit figures there too if a programmatic seam exists. Confidence: 0.6
- For status-line data, prefers static reads from existing config/frontmatter files (role from `.carol-role`) over wiring live event-stream reads — but revised this for the model after noticing `/model` switches didn't repaint the line, now wanting the model to come from the live cmd API/event stream. Confidence: 0.4
- Wants the session status line to include the role's background color (defined in the agent's frontmatter, like other "primaries") and to dynamically follow role switches — model, role, and color should all update live on a switch rather than staying pinned to launch-time values. Confidence: 0.6
- Prefers a single SSOT mechanism for role switching: the three primaries (COUNSELOR / MACHINIST / ORACLE — and only these three) should each be activated through one switch point that atomically flips model, badge, and prompt in the same stroke (setModel + role write + status repaint + prompt injection), rather than separate mechanisms or keeping model a separate concern from role. Confidence: 0.8
- Prefers the status line (and derived UI state) to be a pure function of data — never hardcoded — rendered by a single `updateStatus()` that reads from the SSOT data every render, with all triggers being thin callers of it; editing the source data (e.g., a role/agent file) should trigger a repaint so the line never drifts from its data. Confidence: 0.8
- Prefers the session status line to render immediately beneath the input prompt (adjacent, no vertical gap) matching Claude Code's layout, rather than anchored in a detached bottom footer — surfaced by sending side-by-side screenshots of both tools to highlight the layout discrepancy. Confirmed as a priority when he issued "fix both issues" after the gap was deemed unfixable, indicating he still wanted it remedied. Confidence: 0.8
- Expects global/user-level settings to be the effective authority, and is surprised when a project-scope config file silently overrides them (e.g., a stale `~/.carol/.commandcode/settings.json` pinning `permissions.defaultMode: "default"` over the user-level `auto-accept`); dislikes stale scoped overrides and stale allow-lists pinning behavior contrary to the global default. Confidence: 0.6
- Dislikes being re-asked to choose among options when the stated directive already determines the action — responds tersely to long explanations and decision menus ("just add to deny list. what's the issue?") and wants the agent to just execute the implied fix rather than re-explain trade-offs or offer A/B choices. Confidence: 0.6
- Wants Command Code to launch in full bypass (`--yolo`) mode by default — beyond `auto-accept`, which still prompts for arbitrary mutating shell — persisted via a shell wrapper in the synced zshrc so every session starts prompt-free; still expects deny rules and the destructive-command PreToolUse hook to gate blocked operations under bypass. Confidence: 0.8
- Wants the status line's model to reflect the live/runtime model from the cmd API (event stream, e.g. `model_request_start`), not the static agent-frontmatter seat — after noticing `/model` switches didn't repaint, asked "why not reading from cmd api?", i.e. the model must be read live so switches update immediately. Confidence: 0.7
- Expects a deliberate model switch (via `/model`) to actually change the running model and be reflected in the status label — treats it as a bug when, after restart + switch + reload, the label still shows the old model; dislikes an agent's pinned frontmatter seat silently overriding an explicit session-level switch — reconfirmed when a model switched in a different session still ran as the pinned `deepseek-v4-pro` and he called it "confusing". Confidence: 0.7
- Expects configuration settings (e.g., model selection) to have a clear, predictable scope — global or per-session — and is confused when the effective behavior is neither; dislikes an implicit third mechanism (per-agent frontmatter pin) that overrides an explicit selection without being surfaced. Confidence: 0.6
- When behavior deviates from expectations, asks pointed "why" questions ("why not auto-allowed?", "why always blocked after deny?", "where did it read from? why not reading from cmd api?", "why it does not update?") — expects root-cause diagnosis of which mechanism or state actually produced the behavior, not just a patch on top. Confidence: 0.65
- Verifies changes hands-on between rounds (restart session, switch model, /reload, launching cmd in a directory) and reports back precise observations of what still didn't work — including flatly correcting the agent's "it's already working" conclusions with firsthand observation ("it's not"); treats his own runtime observation as ground truth over the agent's static config analysis, and expects fixes to hold across restarts/reloads with continued iteration until they do. Has explicitly demanded the agent not second-guess him ("dont second guess me. i ran after your changes") — when he reports post-change behavior, take the report as fresh, correct primary evidence and debug from there; do not explain it away as stale sessions, old shells, or launch-context user error before proving it. Confidence: 0.85
- Shell config lives at `~/.config/zsh/zshrc` and is synced across machines — cross-platform changes go there in `$OPERATING_SYSTEM`-conditional blocks, with Windows mapped to the `cmdc` binary variant (e.g., launch wrappers/aliases for `cmd`/`cmdc`). Confidence: 0.6
m changes go there in `$OPERATING_SYSTEM`-conditional blocks, with Windows mapped to the `cmdc` binary variant (e.g., launch wrappers/aliases for `cmd`/`cmdc`). Confidence: 0.6
.g., launch wrappers/aliases for `cmd`/`cmdc`). Confidence: 0.6
